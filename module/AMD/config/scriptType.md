[scriptType](http://requirejs.org/docs/api.html#config-scriptType) 用于设置插入的异步 `<script>` 标签的 `type` 属性：

```js
// scriptType 默认为 'text/javascript'
require.config( {
    scriptType : 'text/javascript;version=1.8'
} )
```
