{
  "name": "fiddle-grunt-inside-tasks",
  "scripts": {
    "test": "mkdir js && touch js/{a,b,c}.js && grunt t0 t1:m:n t2 && rm -rf js"
  },
  "dependencies": {
    "eslint": "~3.2.2",
    "eslint-config-pwn": "~5.0.4",
    "grunt": "~1.0.1",
    "lodash.noop": "~3.0.1"
  }
}
