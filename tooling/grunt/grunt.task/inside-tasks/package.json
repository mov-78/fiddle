{
  "name": "fiddle-grunt-inside-tasks",
  "scripts": {
    "test": "mkdir src && touch src/{a,b,c}.js && grunt task0 task1:m:n task2 && rm -rf src"
  },
  "dependencies": {
    "eslint": "~3.2.2",
    "eslint-config-pwn": "~4.3.0",
    "grunt": "~1.0.1",
    "lodash.noop": "~3.0.1"
  }
}
